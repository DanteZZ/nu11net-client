___k.proc = {
	list:Array(),
	run:function(path,attrs) { // Запустить процесс
		
		list.push()
	}
};

__process = class {
	#ctx = null;
	constructor(path,attrs,actx = false) {
		let ctx = {
			__vm: __vm,
			import:___k.importLibrary,
			_process: {
				send: (str)=>__CLI.printLn(str)
			}
		};
		this.#ctx = __thread.mk(ctx);
		__thread.runScript()
	}
}