___k.proc = {
	list:Array(),
	run:function(path,attrs) { // Запустить процесс
		let ctx = {
			__vm: __vm,
			__
		};
		list.push()
	}
};

__process = class {
	#ctx = null;
	constructor(path,attrs,actx = false) {
		let ctx = {
			__vm: __vm,
			__: ___k
		};
		this.#ctx = __thread.mk(ctx);
		
		__thread.runScript()
	}
}