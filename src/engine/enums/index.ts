export enum eInterface {
  display = "display",
  input = "input",
  storage = "storage",
}

export enum eConnectableInterface {
  ethernet = "ethernet",
}

export enum ePortInterface {
  usb = "usb",
}

export enum eDevice {
  networkSocket = "networkSocket",
  pc = "pc",
}

export enum eProtableDevice {
  usbStorage = "usbStorage",
}

export enum eConnectionType {
  twistedPair = "twistedPair",
}
